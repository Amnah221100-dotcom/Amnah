import { reportTemplates } from '../lib/platform';
export default function ProductionCenter({lang}:{lang:'ar'|'en'}){const ar=lang==='ar';return <div className="production-center"><section className="ops-card"><h3>{ar?'طابور الإنتاج':'Production Queue'}</h3><div className="production-flow">Draft → Analyst Review → QC → Supervisor Approval → Ready → Published</div></section><section className="ops-card"><h3>{ar?'قوالب التقارير':'Report Templates'}</h3><div className="template-grid">{reportTemplates.map(t=><button key={t}>{t}</button>)}</div></section><section className="ops-card"><h3>{ar?'الإجراءات السريعة':'Quick Actions'}</h3><div className="template-grid"><button>Quick Product</button><button>Period Comparison</button><button>Run QC</button><button>Print / PDF</button><button>Email</button><button>Save Version</button></div></section></div>}
