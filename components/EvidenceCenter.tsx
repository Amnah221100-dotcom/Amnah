'use client';
import {demoFindings,demoSources} from '../lib/demo-data';
export default function EvidenceCenter({lang}:{lang:'ar'|'en'}){const ar=lang==='ar';return <div className="module-page"><h1>{ar?'مركز القضايا والأدلة':'Cases & Evidence Center'}</h1><div className="analysis-grid"><section className="ops-card"><h3>{ar?'لوحة الأدلة':'Evidence Board'}</h3><p>Optical · SAR · Terrain · AIS · GNSS · OSINT</p><p>{ar?'كل عنصر يحتفظ بالمصدر والوقت والموقع والموثوقية.':'Every item retains source, time, location and reliability.'}</p></section><section className="ops-card"><h3>{ar?'النتائج والفرضيات':'Findings & Hypotheses'}</h3>{demoFindings.map(f=><p key={f.title}><b>{f.status}</b> · {f.title} · {f.confidence||'—'}%</p>)}</section><section className="ops-card"><h3>{ar?'صحة المصادر':'Source Health'}</h3>{demoSources.map(s=><p key={s.name}>{s.name} · <span className="passed">{s.health}</span></p>)}</section><section className="ops-card"><h3>{ar?'سلسلة الدليل':'Chain of Evidence'}</h3><p>Finding → Evidence → Dataset → Source → Time → Location → Model → Reviewer</p></section></div></div>}
