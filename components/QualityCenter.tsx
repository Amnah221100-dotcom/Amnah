import { qcDomains } from '../lib/platform';
export default function QualityCenter({lang}:{lang:'ar'|'en'}){const ar=lang==='ar';return <div className="qc-center"><div className="qc-score"><b>94%</b><span>{ar?'درجة الجودة':'QC Score'}</span></div><div className="qc-grid">{qcDomains.map((d,i)=><div className="qc-domain" key={d}><strong>{d} QC</strong><span className={i===4?'warning':'passed'}>{i===4?'WARNING':'PASSED'}</span><small>{i===4?(ar?'دليل واحد يحتاج مراجعة':'One evidence item needs review'):(ar?'تم اجتياز الفحوص':'Checks passed')}</small></div>)}</div><div className="workflow"><b>{ar?'سير الاعتماد':'Approval Workflow'}</b><span>Draft → Analyst Review → QC → Supervisor Approval → Approved → Published</span></div></div>}
